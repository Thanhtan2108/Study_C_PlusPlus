/*
Thư viện cmath có sẵn các hàm toán học để tính toán

ceil() : hàm làm tròn lên
floor() : hàm làm tròn xuống
round() : hàm làm tròn đến số nguyên gần nhất
abs() : hàm trị tuyệt đối
fabs() : hàm trị tuyệt đối
sprt() : hàm tính căn bậc 2
cbrt() : hàm tính căn bậc 3
fmax() : tìm max trong 2 số
fmin() : tìm min trong 2 số
pow() : hàm mũ
*/

#include <iostream>
#include <cmath>

using namespace std;

int main() {

}